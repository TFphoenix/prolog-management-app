trace
domains
  Lista=integer*
predicates
  citlis(Lista)
clauses
  citlis([H|T]):-
    write("Element:"),
    readint(H),
    nl,
    H<>0,
    citlis(T).
 citlis([]).
goal
 citlis(Sir),
 write(Sir).  
 